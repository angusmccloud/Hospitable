{
  "name": "hospitable-backend",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -p .",
    "sam:build": "npm run build && sam build",
    "deploy:guided": "npm run sam:build && sam deploy --guided",
    "deploy": "npm run sam:build && sam deploy"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.992.0",
    "@aws-sdk/client-lambda": "^3.995.0",
    "@aws-sdk/client-sqs": "^3.992.0",
    "@aws-sdk/client-ssm": "^3.992.0",
    "@aws-sdk/lib-dynamodb": "^3.992.0",
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.160",
    "@types/node": "^20.17.0",
    "typescript": "^5.9.3"
  }
}
